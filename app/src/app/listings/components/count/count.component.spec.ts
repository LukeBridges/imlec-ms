import {CountComponent} from './count.component';

describe('CountComponent', () => {
  test('should create', () => {
    expect(CountComponent).toBeTruthy();
    expect(new CountComponent()).toBeTruthy();
  });
});
